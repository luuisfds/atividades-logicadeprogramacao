programa { // Calcular a soma dos primeiros numeros naturais de um número
  funcao inicio() {
    inteiro numero, soma

    escreva("Escolha um número: ")
    leia(numero)

    soma = ((numero * (numero + 1))/2)

    escreva("A soma dos numeros inteiros é ", soma)
  }
}
